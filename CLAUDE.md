# CLAUDE.md — guided-notes-generator

> 每個 session 自動載入。**本專案目前＝規劃中，尚未動工。**

## 分工（William 2026-09-02 更新：回到三方慣例）

**Claude 實作 → Codex 複審（循環到通過）→ Grok 複審後掃 → 有發現就修、Codex 對新 head 再審 → 合併。**

〔沿革：2026-09-01 曾指定「Grok 實作→Codex 複審→Claude 掃描」，理由是省 Claude 額度；
次日 William 改回與 webapp／影片線相同的慣例分工——Grok 沒有 repo 與工具，「Grok 實作」的實際
成本是 William 本人當傳輸層逐輪複製貼上，對他是最重的負擔。原「盲測＝Grok 實作試鏡」隨之作廢，
盲測回歸單一目的：測準則。〕

- **Codex＝唯一的獨立審查者**。審查結論逐字三選一：**通過／需修改後再審／不可合併**，
  並附本輪來源字串（誰審的、基準 commit）。
  ⚠️ 誠實劃界（Codex r1）：機械閘只守**實作者≠審查者**，不守「審查者必須是 Codex」——
  後者是規矩不是鎖（閘會放行 Claude／Grok 這種組合；不縮限角色表，是因為分工會變、閘的不變量不該跟著變）。
- **Grok＝複審後掃**：Codex 給出「通過」之後、合併之前，把該版本的 diff（`base..head` 兩顆寫死 SHA）
  抽給 Grok 掃一遍，**每支只掃一次**。⚠️ 順序是設計，別改回「送審前掃」：掃在通過之後，
  撈到的每一條屬實發現就是複審漏掉的證據，不必自評。掃出東西修完＝head 變了，Codex 要對新 head 重新給結論。
  掃的結果（原文＋逐條判定：屬實·已修／屬實·進待辦／誤報）附進 PR 描述固定小標 `### Grok 複審後掃`；
  掃不成就記一行「未執行：<原因>」，不擋合併。
- **Grok 用材料制**：不進 repo、不進任何樹；材料（diff＋指示）抽出來給它。
  ⚠️ 本 repo **沒有** webapp 那套沙箱掃描腳本——別宣稱有。等本 repo 有產品程式再談要不要搬。
- **合併由 Codex 代執行**（實作者不按自己的合併鍵；同 webapp 對稱原則）。
- **審查回饋處置（沿用 webapp，William 2026-09-02 指示套用）**：「誰拍板」與「值不值得改」是兩個判斷。
  **誰拍板＝Claude**，例外三條一律停下來問 William——①Claude 自己不確定 ②動到**判準** ③動到**架構**。
  例外清單的完整版與界線表＝webapp `AGENTS.md`「審查回饋處置」節，**本檔不複述**（會漂的不是規則，是複本）；
  本專案的對應：webapp 的「金額口徑」在這裡讀作**挖空準則條文、盲測門檻、閘門定義**（動到就是動判準）。
  **值不值得改**：只有「假綠／文件保證撐不住／真的產線 bug」三種才回頭改，其餘寫待辦、不再為它多開一輪；
  問法＝「這一輪提的，是使用者會受害，還是考題可以更漂亮？」超過 7 輪自問是不是方向走偏（這題不丟給 William）。
- 操作細節（五步審查循環、唯讀審查紀律、拋棄式審查樹、釘住 commit、`codex exec` 指令與模型設定）
  ＝理財 webapp 的 `REVIEW-AND-MERGE.md`（`../榮祥森（投資理財）/REVIEW-AND-MERGE.md`），本檔刻意不重抄
  （會漂的不是規則，是複本）。⚠️ 只取共用的審查紀律；webapp 專屬的合併八閘、資料紅線、
  沙箱制**不適用**本 repo（這裡沒有那些腳本與考題）。本 repo 自己的合併前機械閘只有一道：
  `node scripts/check-pr-collab-fields.js <N>` 退出碼 0。
- **審查一律在拋棄式樹跑**（`/private/tmp/codex-review-pr<N>`，發射者備與收），不動主目錄——
  主目錄是 William 看檔案的地方（webapp 兩次實測：主目錄被切走，使用者端的東西就壞）。
- **專案慣例一律寫進本檔，不寫進 Claude 的 memory**——Codex 看不到 memory（webapp 同款教訓）。
- **跟 William 的回報只講四件事**：目標是什麼／達成了沒／有沒有改變／需要他裁示什麼。白話、
  細節他問才講（他 2026-09-02 在本專案原話：「看不太懂，也沒有精神看」）。
  錯誤、風險、需要他動手的事照報不減；PR 說明等正式文件照各自規格寫，不套本條。
- 發現 Grok／Codex 的改動未 commit：先審查再 commit，訊息標明出處（`Co-Authored-By: Codex` 等）。
- 給 Codex 的提示詞避開「繞過／繞法／軍備競賽」等措辭（webapp #517 踩過：審查跑完才被內容過濾器擋下，
  446k token 全燒）；改用「未被判準涵蓋到的等價寫法」。

## 機械閘（唯一一道）

`協作欄位（實作者 ≠ 獨立審查者）`——PR 說明五欄齊全、實作者≠審查者、基準版本＝目前 head。
main 已開分支保護（含 `enforce_admins`），設定值與驗證紀錄見 `docs/branch-protection.md`。
⚠️ **這道閘只管 PR 說明，不管內容對錯**；本 repo 沒有自動考題，內容正確與否靠審查與回歸語料庫。

## 鐵則

1. **不寫產品程式**，直到 PLAN.md 的閘門 1（盲測）與閘門 2（付費驗證）由 William 明示通過。
   實驗性腳本（如盲測用的一次性 prompt）可以寫，放 `experiments/`，不算動工。
<<<<<<< HEAD
2. **挖空準則的正本在 Notion**：「十科及格計畫 › 引導式筆記・挖空準則」
   （https://app.notion.com/p/3ce9485922f581319bceef4481ac587d ）。
   動工時凍結一份進 `docs/blanking-rules.md`；在那之前 repo 不留副本（防漂移）。
3. 檔名一律**英文小寫、無空格**（連字號分詞）；文件內文用中文。
   例外＝生態系慣例的根目錄大寫檔（CLAUDE.md、PLAN.md、README.md、REVIEW-BRIEF.md 這一類）。
   （「小寫、無空格」2026-09-03 補明文——原本只寫「英文」，但 .gitignore 的防線以此為前提，
   宣告要跟上實況；Codex r20 抓到落差、r21 抓到例外沒列與日期寫錯。）
=======
2. **挖空準則的正本在 repo**：`docs/blanking-rules.md`。
   （William 2026-09-01 改判：準則會隨製作一路調整，這種東西要有 diff、blame 與審查閘。
   原本放 Notion、repo 不留副本，是為了防漂移——但防漂移的正解是**正本只有一份**，不是「放在版控外」。）
   ⚠️ Notion 那頁（十科及格計畫 › 引導式筆記・挖空準則）**已降級**：標題與頁首自稱「v1 快照、不再維護」
   並指回 repo，但**內文保留移交當時的全文快照**（2026-09-01 14:48 實測確認）。
   快照會隨 repo 演進而過時，那是預期的——**查準則現況只看 repo**，別處要用一律指回來、不要再複製全文。
   ⚠️ **改準則＝改演算法**，適用下面鐵則 6。條號 `A1`–`D3` 是穩定 ID，被盲測、評分表與未來的回歸語料庫
   逐條引用，**不可重編**（作廢的保留號碼標作廢，新增的往後接）。
3. 檔名一律**英文小寫、無空格**（連字號分詞）；文件內文用中文。（「小寫、無空格」2026-09-04 補明文——原本只寫「英文」，但 repo 實況與 .gitignore 的防線都以此為前提，宣告要跟上實況；Codex r20 抓到落差。）
>>>>>>> 2913666 (挖空準則正本搬進 repo：docs/blanking-rules.md)
4. 版權紅線：工具定位＝使用者為**自己的學習**生成個人筆記；不做、不助長「販售他人內容的衍生筆記」。
5. 本專案歸「不簡單・學習神殿」線；決策脈絡見教育藍圖（Notion《十科及格計畫》）。
6. **動到挖空／提煉演算法的 PR，驗收必須附回歸語料庫命中率**（改前 vs 改後）。
   本專案的產出是「挖空品質」，單元測試測不出來——品質退步不會報錯，數字是唯一抓得到「靜靜地錯」的東西。
