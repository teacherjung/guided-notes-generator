node_modules/
.DS_Store
*.log

# 盲測套件的產物與素材（版權紅線＋準則衍生物）。
# 規則放在根：套件自己的 .gitignore 只存在部分分支，切分支時規則會消失、
# 殘留的未追蹤檔案就裸奔——2026-09-02 兩份課本 PDF 就是這樣被 add -A 吞進歷史的。
experiments/**/run/

# iCloud／Finder 產生的複製檔（「名稱 N」「名稱 N.ext」）——r17 抓到「 2」、清完之後
# iCloud 又長出「 3／4／5」再次被 add -A 吞進（r18 抓到）。桌面資料夾會不斷長出這種檔：
# pattern 涵蓋任意數字尾碼，且本 repo 從此不用 add -A（改明確清單 add）。
* [0-9].*
* [0-9]
* [0-9][0-9].*
* [0-9][0-9]
