node_modules/
.DS_Store
*.log

# 盲測套件的產物與素材（版權紅線＋準則衍生物）。
# 規則放在根：套件自己的 .gitignore 只存在部分分支，切分支時規則會消失、
# 殘留的未追蹤檔案就裸奔——2026-09-02 兩份課本 PDF 就是這樣被 add -A 吞進歷史的。
experiments/**/run/

# 檔名含空格＝不是本 repo 的正當檔案（CLAUDE.md 鐵則 3：檔名一律英文小寫、無空格）。
# 這條同時擋掉 iCloud／Finder 的「名稱 N」複製檔（r17 抓到「 2」、r18 抓到「 3/4/5」、
# r19 指出列舉數字位數擋不完——複製檔一定含空格，擋空格就是擋全類）。
# 本 repo 並從此不用 add -A（改明確清單 add）。
* *
