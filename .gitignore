node_modules/
.DS_Store
*.log

# 盲測套件的產物與素材（版權紅線＋準則衍生物）。
# 規則放在根：套件自己的 .gitignore 只存在部分分支，切分支時規則會消失、
# 殘留的未追蹤檔案就裸奔——2026-09-02 兩份課本 PDF 就是這樣被 add -A 吞進歷史的。
experiments/**/run/

# iCloud／Finder 產生的複製檔（「名稱 2.ext」）——2026-09-04 曾被 add -A 吞進 commit，
# 七份複製檔裡四份「正本」各自宣稱唯一，審查 r17 抓到。桌面資料夾會自己長出這種檔，擋在這裡。
* 2.*
*\ 2
