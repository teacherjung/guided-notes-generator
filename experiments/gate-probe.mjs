// 一次性探針（不是產品程式）：故意送壞的 PR 說明，確認協作欄位閘真的會擋。
// 本專案的老教訓：護欄不能只看它綠燈，要先弄壞一次看警報響不響。
import { problemsOf, ROLES } from '../scripts/check-pr-collab-fields.js';
const cases = [
  ['空模板（五欄全空）＝必須擋', `## 協作欄位\n- **實作者**：\n- **獨立審查者**：\n- **基準版本**：\n- **預計修改的共享檔案**：\n- **這支若完全失敗，最糟失去什麼**：`],
  ['Grok 實作／Codex 審＝本專案正常案，必須放行', `- **實作者**：Grok\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：一版演算法重寫`],
  ['自審（Grok 審 Grok）＝必須擋', `- **實作者**：Grok\n- **獨立審查者**：Grok\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`],
  ['角色亂寫（Gemini）＝必須擋', `- **實作者**：Gemini\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`],
  ['括號裡藏第二角色＝必須擋', `- **實作者**：Grok（Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`],
];
console.log('ROLES =', ROLES.join('／'));
for (const [name, body] of cases) {
  const p = problemsOf(body);
  console.log(`\n[${p.length ? '擋下' : '放行'}] ${name}`);
  p.forEach((x) => console.log('   ・' + x));
}
