// 一次性探針（不是產品程式）：送格式偏差的 PR 說明樣本，確認協作欄位檢查會提醒補正。
// 本專案的老教訓：檢查器不能只看它通過正常案，要先送偏差樣本確認提醒有出現。
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { problemsOf, staleBaseProblems, ROLES } from '../scripts/check-pr-collab-fields.js';
const cases = [
  ['空模板（五欄全空）＝應退回', `## 協作欄位\n- **實作者**：\n- **獨立審查者**：\n- **基準版本**：\n- **預計修改的共享檔案**：\n- **這支若完全失敗，最糟失去什麼**：`, 'block'],
  ['Grok 實作／Codex 審＝合法組合（實作者≠審查者；本專案預設是 Claude 實作，但閘不管預設，只管不變量），必須放行', `- **實作者**：Grok\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：一版演算法重寫`, 'pass'],
  ['自審（Grok 審 Grok）＝應退回', `- **實作者**：Grok\n- **獨立審查者**：Grok\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['角色亂寫（Gemini）＝應退回', `- **實作者**：Gemini\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['括號內多出第二個角色名＝應退回', `- **實作者**：Grok（Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下三案＝Codex r1（基準 f730cf8）實測繞過後補的，修完轉常設案例
  ['括號內角色名被空白分隔（Co dex）＝應退回', `- **實作者**：Claude（Co dex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['同名欄位重複出現（兩個實作者欄）＝應退回', `- **實作者**：Claude\n- **實作者**：Codex\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['自由欄填零寬空白（U+200B）＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：\u200b\n- **這支若完全失敗，最糟失去什麼**：\u200b`, 'block'],
  // 以下三案＝Codex r2（基準 768c6c3）實測繞過 r1 修法後補的（列舉分隔符→關門）
  ['括號內角色名被斜線分隔（Co/dex）＝應退回', `- **實作者**：Claude（Co/dex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['全形斜線分隔（Co／dex）＝應退回', `- **實作者**：Claude（Co／dex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['角色名分置兩個括號（Co）（dex）＝應退回', `- **實作者**：Claude（Co）（dex）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r3（基準 ef9e1eb）：HTML entity 與標籤在渲染層重組角色名。
  // r3 起 canonicalRole 改嚴格模式（值必須恰好等於角色名，不接受任何加註），這兩案連同
  // 所有加註型寫法一起關掉；「Claude（已看過）」這種無害加註被一併拒收是刻意的代價。
  ['HTML entity 表示的角色名（Co&#100;ex）＝應退回', `- **實作者**：Claude（Co&#100;ex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['HTML 標籤穿插的角色名（Co<em>d</em>ex）＝應退回', `- **實作者**：Claude（Co<em>d</em>ex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['無害加註（Claude（已看過））＝嚴格模式下也應退回', `- **實作者**：Claude（已看過）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r4（基準 5a9045f）：加註搬到下一行（markdown 清單續行，含 lazy continuation）
  ['角色欄下出現縮排續行＝應退回', `- **實作者**：Claude\n  （Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['角色欄頂格 lazy 續行＝應退回', `- **實作者**：Claude\n（Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r5（基準 52235cf）：空行段落／巢狀子清單／U+2028 行分隔。
  // r5 起改白名單（五欄連續五行、順序照模板、行間零容忍），黑名單五修五繞到此為止。
  ['角色欄後空行＋縮排段落＝應退回', `- **實作者**：Claude\n\n  （Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['角色欄下巢狀子清單＝應退回', `- **實作者**：Claude\n  - Codex 也有動\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['U+2028 作行分隔符的續行＝應退回', `- **實作者**：Claude\u2028（Codex 也有動）\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['五欄之間插入其他行＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n說明一下\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['CRLF 行尾的正常五欄＝應通過', `- **實作者**：Claude\r\n- **獨立審查者**：Codex\r\n- **基準版本**：abc1234\r\n- **預計修改的共享檔案**：無\r\n- **這支若完全失敗，最糟失去什麼**：無`, 'pass'],
  // 以下＝Codex r6（基準 309e7ed）：code 語境旁路——範例區塊被誤認正式欄位、行內 code 藏註解
  ['fenced code block 裡的五欄＝範例不是欄位，應退回', '說明\n```md\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n```', 'block'],
  ['行內 code 內含註解標記＝應退回', `- **實作者**：Claude \`<!-- Codex 也有動 -->\`\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['跨行註解覆蓋到審查者欄＝應退回', `- **實作者**：Claude <!--\n- **獨立審查者**：Codex -->\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r7（基準 b6f26c7）：跨行 code span 與 fence 邊界細節。
  // r7 起加第二道白名單：五欄必須在說明最前面——語境開啟符都要放在五欄前面才生效，
  // 把前面的空間拿掉，整個語境旁路家族（含 r7 標記為待辦的 raw HTML <pre>）一次關門。
  ['跨行 code span 包住五欄＝範例不是欄位，應退回', '範例：`\n實作者：Claude\n獨立審查者：Codex\n基準版本：abc1234\n預計修改的共享檔案：無\n這支若完全失敗，最糟失去什麼：無\n`', 'block'],
  ['fence 開閉長度不匹配＝應退回', '````\n假關\n```\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無', 'block'],
  ['raw HTML pre 包五欄＝應退回', '<pre>\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n</pre>', 'block'],
  ['標題＋五欄在最前＝正常案，應通過', '## 協作欄位\n\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 這一次實際改了什麼\n\n說明文字', 'pass'],
  // 以下＝Codex r8（基準 77c74d8）：欄位行自身的值開啟渲染語境
  ['非法標題行尾帶 HTML 標籤＝應退回', `##協作欄位 <details>\n實作者：Claude\n獨立審查者：Codex\n基準版本：abc1234\n預計修改的共享檔案：無\n這支若完全失敗，最糟失去什麼：無\n</details>`, 'block'],
  ['無清單標記且值尾帶 HTML 標籤＝應退回', `## 協作欄位\n實作者：Claude\n獨立審查者：Codex\n基準版本：abc1234 <details>\n預計修改的共享檔案：無\n這支若完全失敗，最糟失去什麼：無\n</details>`, 'block'],
  ['清單項值尾帶 HTML 標籤＝應退回', `## 協作欄位\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234 <details>\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r9（基準 a419e63）：偽清單標記讓跨行語法重獲吞行力
  ['十位數偽有序標記＋圖片語法跨行＝應退回', `1234567890. **實作者**：Claude\n1234567890. **獨立審查者**：Codex\n1234567890. **基準版本**：abc1234 ![\n1234567890. **預計修改的共享檔案**：無\n1234567890. **這支若完全失敗，最糟失去什麼**：無](https://x.png)`, 'block'],
  ['「- 」後多塞空白（渲染成清單內 code）＝應退回', `-     **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['欄位值含連結開啟符＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：[docs\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r10（基準 d1db572）：刪除註解造成的文字拼接合成假結構
  ['行內註解拼接成清單標記＝應退回', `-<!--x--> **實作者**：Claude\n-<!--x--> **獨立審查者**：Codex\n-<!--x--> **基準版本**：abc1234\n-<!--x--> **預計修改的共享檔案**：無\n-<!--x--> **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['行內註解拼接成標題＝應退回', `##<!--x--> 協作欄位\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r11（基準 69cf471）：GFM 不承認的偽註解被寬鬆 regex 當註解抹掉
  ['偽註解內含標籤＝應退回', `<!--><details>-->\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['內含雙連字號的偽註解＝應退回', `<!-- a--b --><details>\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['正規多行註解＋五欄＝應通過', `<!--\n填寫說明\n-->\n\n## 協作欄位\n\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'pass'],
  // 以下＝Codex r12（基準 d864319）：code 與註解記號重疊時，遮蔽順序讓合法性誤判
  ['反引號與註解記號在前導區重疊＝模稜，應退回', '<!-- \u0060-->\u0060<details> -->\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無', 'block'],
  // 以下＝Codex r13（基準 94e432b）：縮排 code／~~~ fence 同樣能改寫註解邊界
  ['縮排 code 改寫註解邊界＝應退回', '<!--\n    -->\n<details> -->\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n</details>', 'block'],
  // 以下＝Codex r14（基準 f9627c3）：兩處縮排判定不一致的縫隙
  ['tab＋空白縮排行改寫註解邊界＝應退回', '<!--\n\t -->\n<details> -->\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n</details>', 'block'],
  // 以下＝Codex r15（基準 cf9f8ce）：縮排定義照 CommonMark 列計算後補的案例
  ['1 空格＋tab 縮排（展開後＝4 列）改寫註解邊界＝應退回', '<!--\n \t-->\n<details> -->\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無', 'block'],
  // 以下＝Codex r16（基準 b55deb9）：不成對／字中裝飾符被兩站分剝洗成合法
  ['不成對星號 *Claude＝應退回', `- **實作者**：*Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['字中底線 Cl_aude＝應退回', `- **實作者**：Cl_aude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['成對粗體 **Claude**＝應通過', `- **實作者**：**Claude**\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'pass'],
  // 以下＝Codex r17（基準 035f90d）：剝除順序的洞——NFKD 先跑折全形、層間 trim 抹內側空白
  ['delimiter 內側帶空白 ** Claude **＝應退回', `- **實作者**：** Claude **\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['全形星號 ＊＊Claude＊＊＝應退回', `- **實作者**：＊＊Claude＊＊\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['全形底線 ＿＿Claude＿＿＝應退回', `- **實作者**：＿＿Claude＿＿\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r18（基準 852aa49）：折疊式正規化把「渲染保留字形」的寫法漂白成合法
  ['全形字母 Ｃｌａｕｄｅ＝應退回', `- **實作者**：Ｃｌａｕｄｅ\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['重音字母 Cláude＝應退回', `- **實作者**：Cláude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['數學字母 𝐂𝐥𝐚𝐮𝐝𝐞＝應退回', `- **實作者**：𝐂𝐥𝐚𝐮𝐝𝐞\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['刪除線組合字＝應退回', `- **實作者**：C̶l̶a̶u̶d̶e̶\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['雙向控制碼＝應退回', `- **實作者**：\u202eedualC\u202c\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r19（基準 59baccc）：任意巢狀剝除讓交錯 delimiter 假綠
  ['交錯巢狀 _*_*Claude*_*_＝應退回', `- **實作者**：_*_*Claude*_*_\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['粗斜 ***Claude***＝應通過', `- **實作者**：***Claude***\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'pass'],
  // 以下＝Codex r22（基準 8525066）：HTML character reference 讓原文與渲染分岔
  ['基準版本欄帶 entity 假 SHA＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234 / &#x36;&#x66;&#x32;&#x39;&#x35;&#x30;&#x39;\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['自由欄填 &nbsp; 假非空＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：&nbsp;\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['正文區自由使用 &＝應通過', `## 協作欄位\n\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 說明\n\nA&B 公司 &copy; 正文`, 'pass'],
  // 以下＝Codex r23（基準 9a7741f）：刪除線包住 SHA——ASCII 檢查只護角色欄
  ['刪除線包基準版本 ~~SHA~~＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：~~abc1234~~\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['正文區刪除線自由＝應通過', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 說明\n\n~~正文刪除線~~`, 'pass'],
  // 以下＝Codex r24（基準 92fb107）：MathJax（GitHub 擴展，非 GFM）隱藏真值顯示假值
  ['數學式偽造基準版本＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：$\\phantom{92fb107}\\text{9}\\text{a}$\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['正文區數學式自由＝應通過', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 說明\n\n$E=mc^2$`, 'pass'],
  // 以下＝Codex r25（基準 ce35812）：bidi 控制碼在基準版本欄隱真示假（r18 只護了角色欄）
  ['bidi 反序偽造基準版本＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：\u202Ece35812\u202C\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  ['正文區含變體字元＝應通過', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 說明\n\n✍\uFE0F 正文自由`, 'pass'],
  // 以下＝Codex r26（基準 82ea1e1）：黑名單補不完，改真正的允許字元白名單
  ['盲文空白 U+2800 假非空＝應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：\u2800\n- **這支若完全失敗，最糟失去什麼**：\u2800`, 'block'],
  ['前導區 emoji＝白名單外，應退回', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：🙂`, 'block'],
  ['正文區任何字元自由＝應通過', `- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無\n\n## 說明\n\n🙂 \u2800 &copy; $x$ ~~刪~~ 自由`, 'pass'],
  // 以下＝Codex r27（基準 184ae8a）：「空行」也是兩套定義——trim() 認 U+00A0、GitHub 不認
  ['U+00A0 假空行＝應退回', `\u00A0\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
  // 以下＝Codex r28（基準 32600db）：三處空判定歸一後的一致性案例
  ['縮排＋U+00A0 行（三定義一致後屬 code 行）＝應退回', `    \u00A0\n- **實作者**：Claude\n- **獨立審查者**：Codex\n- **基準版本**：abc1234\n- **預計修改的共享檔案**：無\n- **這支若完全失敗，最糟失去什麼**：無`, 'block'],
];

// ＝＝＝ 官方模板本體（Grok 掃 #12：29 輪都在測合成字串，沒人把真模板送進閘——
//       模板改版後這一段自動跟上，「空模板案」不再是手寫仿品）＝＝＝
const TEMPLATE = readFileSync(join(dirname(fileURLToPath(import.meta.url)), '..', '.github', 'pull_request_template.md'), 'utf8');
const FILLED = TEMPLATE
  .replace('- **實作者**：', '- **實作者**：Claude')
  .replace('- **獨立審查者**：', '- **獨立審查者**：Codex')
  .replace('- **基準版本**：', '- **基準版本**：abc1234')
  .replace('- **預計修改的共享檔案**：', '- **預計修改的共享檔案**：無')
  .replace('- **這支若完全失敗，最糟失去什麼**：', '- **這支若完全失敗，最糟失去什麼**：無');
cases.push(['官方模板原樣（未填）＝應退回', TEMPLATE, 'block']);
cases.push(['官方模板照說明填好＝應通過', FILLED, 'pass']);

// ⚠️ 每案帶**機器可驗的預期值**（r15）：舊版只印結果，案例名寫「應退回」但迴圈不驗證，
//    判定反轉照樣 exit 0——探針自己就是「綠燈不代表有在守」的標本。
//    現在：任一案結果與預期不符＝印 FAIL＋exit 1。
console.log('ROLES =', ROLES.join('／'));
let failed = 0;
for (const [name, body, expect] of cases) {
  const p = problemsOf(body);
  const got = p.length ? 'block' : 'pass';
  const ok = got === expect;
  if (!ok) failed++;
  console.log(`\n[${ok ? (got === 'block' ? '擋下' : '放行') : 'FAIL'}] ${name}`);
  p.forEach((x) => console.log('   ・' + x));
}
// ＝＝＝ staleBaseProblems 直測（Grok 掃 #7/#13/#14：合併閘＝problemsOf＋staleBaseProblems
//       兩段，探針原本只測前半——「該擋的全擋」的宣稱要涵蓋整條路徑）＝＝＝
const HEAD = 'abc1234f000000000000000000000000000000ff';
const staleCases = [
  ['基準＝head 前綴', '- **基準版本**：abc1234', 'pass'],
  ['基準＝別的 SHA', '- **基準版本**：deadbee', 'block'],
  ['基準＝無', '- **基準版本**：無', 'block'],
  ['空白拆開的 hex 段（各自不足七碼）', '- **基準版本**：ab c1234', 'block'],
  ['星號拆開的 hex 段', '- **基準版本**：ab*c1234', 'block'],
  ['成對粗體包 SHA', '- **基準版本**：**abc1234**', 'pass'],
];
for (const [name, body, expect] of staleCases) {
  const got = staleBaseProblems(body, HEAD).length ? 'block' : 'pass';
  const okS = got === expect;
  if (!okS) failed++;
  console.log(`[${okS ? (got === 'block' ? '擋下' : '放行') : 'FAIL'}] staleBase：${name}`);
}

if (failed) {
  console.error(`\n✗ ${failed} 案與預期不符`);
  process.exit(1);
}
console.log(`\n✓ ${cases.length + staleCases.length} 案全部符合預期（problemsOf ${cases.length}＋staleBase ${staleCases.length}）`);
